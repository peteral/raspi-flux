apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: raspi-flux
  namespace: flux-system
spec:
  interval: 30s
  ref:
    branch: main
  url: https://github.com/peteral/raspi-flux.git